@model IEnumerable<DearWalletWeb.Odgovor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pitanje.Tekst)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TekstOdgovora)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PripadniStil)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Pitanje.Tekst)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TekstOdgovora)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PripadniStil)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OdgovorId }) |
            @Html.ActionLink("Details", "Details", new { id=item.OdgovorId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OdgovorId })
        </td>
    </tr>
}

</table>
