@model IEnumerable<DearWalletWeb.Pitanje>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tekst)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tekst)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PitanjeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PitanjeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PitanjeId })
        </td>
    </tr>
}

</table>
